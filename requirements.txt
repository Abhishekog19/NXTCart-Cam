# ---------------------------------------------------------------
# NXTCart-Cam – Product Recognition System
# Minimal dependencies.  Install with:
#   pip install -r requirements.txt
# ---------------------------------------------------------------

# Core vision & numerics
opencv-python==4.10.0.84
numpy==1.26.4

# TensorFlow Lite runtime (lighter than full TF, no GPU needed)
# On a normal laptop use the wheel below.
# On Raspberry Pi you'll swap this for the ARM wheel from
#   https://github.com/google-coral/pycoral/releases
tflite-runtime==2.14.0

# HTTP download for the TFLite model (used once by embedding_extractor.py)
requests==2.32.3
